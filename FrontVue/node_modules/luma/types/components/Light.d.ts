import * as React from "react";
declare type LightProps = any;
export declare const LightContext: React.Context<{
    x: number;
    y: number;
    z: number;
    minOpacity: number;
    maxOpacity: number;
    diffusion: number;
    intensity: number;
}>;
export declare function Light({ children, x, y, z, minOpacity, maxOpacity, diffusion, intensity, }: LightProps): JSX.Element;
export {};
